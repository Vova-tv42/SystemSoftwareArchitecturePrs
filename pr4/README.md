# Практична робота №4: Керування пам'яттю в Linux

## Складання та запуск

Для автоматичної компіляції всіх завдань використовується скрипт `build.sh`.

**Команда для компіляції усіх файлів:**
```bash
./build.sh
```

---

## task1.c
Програма досліджує максимальний обсяг пам'яті, який можна виділити за один виклик `malloc`. Вона намагається виділити максимально можливе значення для типу `size_t` і виводить результат операції.

**Результат виконання:**


---

## task2.c
Програма демонструє поведінку `malloc` при передачі від'ємного аргументу (який інтерпретується як велике число `size_t`) та наслідки переповнення при множенні цілих чисел, що використовується як аргумент для виділення пам'яті.

**Результат виконання:**


---

## task3.c
Дослідження виклику `malloc(0)`. Програма перевіряє, чи повертає система `NULL` чи унікальний вказівник, який можна безпечно передати у функцію `free`. Для детального аналізу рекомендується запуск через `ltrace ./task3`.

**Результат виконання:**


**Результат виконання з ltrace:**


---

## task4.c
Демонстрація помилки dangling pointer. Програма імітує логічну помилку в циклі, де вказівник не обнуляється після звільнення пам'яті, що призводить до некоректної роботи умови перевірки на наступній ітерації.

**Результат виконання:**


---

## task5.c
Програма демонструє випадок, коли `realloc` не може виділити новий блок пам'яті (через запит на неможливий розмір). Вона демонструє, що в такому випадку початковий блок пам'яті залишається цілим і його потрібно звільняти окремо.

**Результат виконання:**


---

## task6.c
Тестування особливих випадків використання `realloc`: передача `NULL` замість вказівника (поведінка як у `malloc`) та передача розміру `0` (поведінка як у `free`).

**Результат виконання:**


---

## task7.c
Програма демонструє використання функції `reallocarray`. Для порівняння викликів `calloc` та `reallocarray` рекомендується використовувати `ltrace`.

**Результат виконання:**


**Результат виконання з ltrace:**


---

## task_v9.c
Реалізація власного пулу пам'яті (`memory_pool`) для об'єктів фіксованого розміру. Програма порівнює швидкість виділення та звільнення 10.000 блоків пам'яті між стандартним `malloc/free` та власною реалізацією на основі попередньо виділеного масиву.

**Результат виконання:**

