# Практична робота №7: Дослідження, моделювання та нестандартні підходи до аналізу процесів, файлових систем, безпеки та ресурсів в Linux

## Налаштування та запуск

**Компіляція всіх завдань:**

Для компіляції програми використовується скрипт build.sh.
```
./build.sh
```

## Використання файлів програм та пояснення

### task1.c
Програма використовує `popen()`, щоб передати вивід команди `who` до команди `more`. Вона відкриває один потік для читання виводу `rwho` та інший для запису в `more`, копіюючи дані між ними.

**Примітка:** Замість `rwho` було використано сучасну утиліту `who`. 

**Результат виконання:**


### task2.c 
Програма імітує команду `ls -l` у поточному каталозі. Вона зчитує вміст каталогу та для кожного елемента отримує детальну інформацію про права доступу, кількість посилань, власника, групу, розмір та час останньої зміни.

**Результат виконання:**


### task3.c
Програма друкує всі рядки з файлу, що містять слово, передане як перший аргумент.

- **Приклад запуску:** `./task3 <word> <file_name>`

**Аргументи запуску:**
- `word` — слово, яке необхідно знайти (обов'язковий).
- `file_name` — назва або шлях до файлу для пошуку (обов'язковий).

**Результат виконання:**


### task4.c
Виводить список файлів із паузою кожні 20 рядків. Очікує натискання клавіші [Enter] для продовження.
- **Приклад запуску:** `./task4 <file1> [file2 ...]`

**Аргументи запуску:**
- `file1` — шлях до першого файлу для перегляду (обов'язковий).
- `file2 ...` — шляхи до додаткових файлів (опціонально).

**Результат виконання:**


### task5.c 
Виконує рекурсивний обхід усіх підкаталогів, починаючи з поточного, та виводить шляхи до всіх знайдених файлів.

**Результат виконання:**


### task6.c
Перелічує лише підкаталоги в поточному каталозі у алфавітному порядку.

**Результат виконання:**


### task7.c
Знаходить усі `.c` файли та запитує користувача, чи потрібно надати іншим дозвіл на читання (`read permission`).

**Результат виконання:**


### task8.c
Запитує користувача про видалення кожного файлу в поточному каталозі.

**Результат виконання:**


### task9.c
Вимірює час виконання фрагмента коду (великого циклу) у мілісекундах.

**Результат виконання:**


### task10.c
Генерує випадкові числа з плаваючою точкою у діапазонах [0.0, 1.0] та [0.0, n].
- **Приклад запуску:** `./task10 [n]` (де `n` - ваше число)

**Аргументи запуску:**
- `n` — верхня межа діапазону від 0.0 до n (опціонально, за замовчуванням 100.0).

**Результат виконання:**


### task_v9.c
Сканує порти (від 20 до 100) на вказаній IP-адресі без прямого використання сокетів.
- **Приклад запуску:** `./task_v9 <IP>` (наприклад, `./task_v9 127.0.0.1`)

**Аргументи запуску:**
- `IP` — IP-адреса або ім'я хоста для сканування (обов'язковий).

**Результат виконання:**

