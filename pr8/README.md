# Практична робота №8: Системні виклики в UNIX/POSIX (файлові операції, fork(), qsort(), write(), read(), lseek() тощо

## Налаштування та запуск

**Компіляція всіх завдань:**

Для компіляції програми використовується скрипт build.sh.
```
./build.sh
```

## Використання файлів програм та пояснення

### task1.c
Програма демонструє, чи може виклик `write()` повернути значення, відмінне від кількості байтів, що передаються. У прикладі показано, що при спробі запису в закритий канал виклик повертає -1.

**Результат виконання:**


### task2.c
Програма демонструє переміщення покажчика у файлі за допомогою `lseek()` та подальше читання даних за допомогою `read()`. Файл містить послідовність {4, 5, 2, 2, 3, 3, 7, 9, 1, 5}.

**Результат виконання:**


### task3.c
Програма досліджує продуктивність бібліотечної функції `qsort()`, підраховуючи кількість порівнянь для різних типів вхідних масивів (випадковий, відсортований, зворотньо відсортований).

**Результат виконання:**


### task4.c
Програма використовує системний виклик `fork()` для створення нового процесу та виводить ідентифікатор процесу (`pid`), який повертає функція.

**Результат виконання:**


### task_v9.c
Програма демонструє роботу `fork()` усередині циклу `for` із затримкою. Досліджується кількість створених процесів та їхні ідентифікатори.

**Результат виконання:**

